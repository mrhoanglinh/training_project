<html>
  <body>
    <!--show message flag-->
    <div id="blog-table-alerts">
      <%= render 'alerts' %>
    </div>

    <div class="row">
      <div class="form-group" style="padding-left: 20px">
        <h2><%= t "admin.blog.index.title"%></h2>
      </div>
      <div>
        <%= render 'search_form' %>
      </div>

      <div class="form-group" style="padding-left: 50px">
        <%= link_to t("admin.blog.index.add_new"), new_admin_blog_path, class: "btn btn-default btn-primary" %>
      </div>

      <div class="form-group" style="padding-left: 50px">
        <%= paginate @blogs %>
      </div>
      <%= render 'table_blog_list' %>
      <div class="form-group" style="padding-left: 50px">
        <%= paginate @blogs %>
      </div>
    </div>

  </body>
</html>


